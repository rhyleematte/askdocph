# Define the build phases for Railway (Nixpacks)
[phases.setup]
nixPkgs = ["php", "nodejs", "composer"]

[phases.install]
cmds = ["composer install --no-dev", "npm install"]

[phases.build]
cmds = ["npm run prod"]

[start]
cmd = "php artisan serve --host 0.0.0.0 --port $PORT"
