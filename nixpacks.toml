# Use the official Nixpacks variable to hint the PHP version
[variables]
NIXPACKS_PHP_VERSION = '8.1'

[phases.install]
cmds = ["composer install --no-dev", "npm install"]

[phases.build]
cmds = ["npm run prod"]

[start]
cmd = "php artisan serve --host 0.0.0.0 --port $PORT"
